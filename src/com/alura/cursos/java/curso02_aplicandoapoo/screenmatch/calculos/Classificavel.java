package com.alura.cursos.java.curso02_aplicandoapoo.screenmatch.calculos;

public interface Classificavel {
    int getClassificacao();

    //interface serve como um contrato. ""Geralmente"" (nn sempre), não existe implementação
    //interface tbm não pode ser instanciada (criar um new), pois ela serve mais como um contrato
}
